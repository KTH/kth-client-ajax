{
  "name": "@kth/kth-client-ajax",
  "version": "2.0.0",
  "description": "A module for browser clients with helper functions to make ajax calls.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
